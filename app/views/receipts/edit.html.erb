<%= simple_form_for @receipt do |r| %>
  <%= r.input :date, label: 'Data', html5: true %>
  <%= r.input :description, label: 'Descrição' %>
  <%= r.input :value, label: 'Valor' %>
  <%= r.button :submit %>
<% end %>
